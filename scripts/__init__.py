"""
Local package initializer for the `scripts` module.

Having this file ensures that the project-local `scripts` package takes
precedence over any third-party package named `scripts` that might be
installed in the environment.
"""
